To test locally, run the docker application, then run the commands `docker compose pull` then run `docker compose up`. For Docker install and all the other proper readme stuff see the al-folio github page, https://github.com/alshedivat/al-folio.